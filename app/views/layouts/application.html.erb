<!DOCTYPE html>
<html>
  <head></head>
  <body>
    <header class="navbar navbar-fixed-top navbar-inverse">
      <div class="container">
      <nav class="nav navbar-nav navbar-left"></nav>
      <nav>
        <ul class="nav navbar-nav navbar-right" data-hover="dropdown" data-animations="zoomIn fadeIn fadeInUp fadeIn">
        <% if user_signed_in? %>
          <li>
            <%= link_to "glyphicon glyphicon-home Home", user_path(current_user.id) %>
          </li>
          <li>
            <%= link_to "glyphicon glyphicon-user Users", users_path %>
          </li>
          <li>
            <%= link_to"glyphicon glyphicon-book Books", books_path %>
          </li>
          <li>
            <%= link_to "glyphicon glyphicon-log-out logout",destroy_user_session_path, method: :delete %>
          </li>
        <% else %>
          <li>
            <%= link_to "glyphicon glyphicon-sign-up sign-up", new_user_registration_path %>
          </li>
          <li>
            <%= link_to "glyphicon glyphicon-login login", new_user_session_path %>
          </li>
        <% end %>
      </ul>
      </nav>
    </div>
  </header>

    <%= yield %>

   <footer>
        <div class="container">
          <div class="row">
            <div class="col-xs-3 col-xs-offset-9">
              <p>CopyRight Infratop.inc</p>
            </div>
          </div>
        </div>
      </footer>
    </body>
 </html>
